# AI features (image gen, video gen, remove BG) are optional — the editor works without a key.
# Environment API keys are never embedded in the browser bundle.
# Enter optional AI provider keys explicitly in the studio's Settings.
